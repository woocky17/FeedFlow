import { SUPPORTED_LANGUAGES, DEFAULT_LANGUAGE, type Language } from "@/domain/shared";

export const LOCALES = SUPPORTED_LANGUAGES;
export const DEFAULT_LOCALE: Language = DEFAULT_LANGUAGE;
export const LOCALE_COOKIE = "NEXT_LOCALE";
